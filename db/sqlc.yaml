version: "2"
cloud:
  project: "01HEGANSGHZH9RF76Z6HD9KA74"
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    database:
      managed: true
    rules:
      - sqlc/db-prepare
    gen:
      go:
        package: "db"
        sql_package: "pgx/v5"
        out: "db"
